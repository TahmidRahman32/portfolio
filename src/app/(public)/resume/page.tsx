import ResumeBuilder from '@/components/resume/ResumeBuilder';
import React from 'react';

const page = () => {
   return (
      <div>
        <ResumeBuilder />
      </div>
   );
};

export default page;